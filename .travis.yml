sudo: false
language: ruby
rvm:
- 2.5.1
cache: bundler
before_install:
- openssl aes-256-cbc -K $encrypted_602bfab82c29_key -iv $encrypted_602bfab82c29_iv
  -in service-account.json.enc -out service-account.json -d
- gem install bundler -v 1.16.1
before_script:
- psql -c 'create database courier_tweeter_test;' -U postgres
- DB_URL="postgres:///courier_tweeter_test" bundle exec rake db:migrate
services:
- postgresql
addons:
  postgresql: '9.6'
deploy:
  provider: gae
  project: stable-reactor-209402
